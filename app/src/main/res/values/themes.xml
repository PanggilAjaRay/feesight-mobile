<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FeeSight" parent="Theme.MaterialComponents.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/green</item>

        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="toolbarStyle">@style/Theme.Custom.Toolbar</item>
        <item name="android:statusBarColor">@color/green</item>


    </style>
    <style name="settings">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#000000</item>
    </style>


    <style name="Theme.Custom.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/white</item>
    </style>
    <style name="Theme.FeeSight" parent="Base.Theme.FeeSight" />
</resources>