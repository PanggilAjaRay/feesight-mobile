<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FeeSight" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/green</item>
        <item name="android:windowBackground">@color/gulf_blue</item>
        <item name="cardBackgroundColor">@color/midnight_blue</item>
        <item name="tabBackground">@color/gray_blue</item>
        <item name="android:textColor">@color/blue_bell</item>
        <!-- Warna untuk mode gelap -->

        <item name="toolbarStyle">@style/Theme.Custom.Toolbar</item>
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.Custom.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/gulf_blue</item>
    </style>
</resources>